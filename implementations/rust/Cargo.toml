[workspace]
resolver = "2"
members = ["axum", "core", "actix-web"]

[workspace.dependencies]
sqlx = { version = "0.9", default-features = false, features = [
    "runtime-tokio",
    "tls-rustls-ring-webpki",
    "postgres",
    "uuid",
    "chrono",
    "json",
] }
