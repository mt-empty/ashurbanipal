{
  "name": "ashurbanipal-frontend",
  "private": true,
  "version": "0.0.0",
  "description": "Build tooling for frontend/dbviewer.html — bundles frontend/src/ into the single shipped artifact. See CLAUDE.md and docs/frontend-style-guide.md.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.27.1",
    "typescript": "^7.0.2"
  }
}
